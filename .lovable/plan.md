# Plan: Completed

The group selection + remaining countdown feature has been implemented.

## Implemented Features

1. **NeighborhoodReservationModal** - Added `bedsAssigned` input field with validation, enhanced group selector showing remaining counters formatted as `{groupName} ({dates}) • חניכים נשאר: {remaining}`

2. **TentDetailModal** - Added group linking capability for individual tent bookings with VIP/participant distinction

3. **AdminGroups** - Shows remaining staff and participant counters in group list cards

4. **useGroupAllocation** - Added `canAllocate` helper for pre-validation
